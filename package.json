{
  "name": "snapserve-vault-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Snapserve Vault Unified Monorepo (Single Service Deployment)",
  "scripts": {
    "build": "npm --prefix web install --include=dev && npm --prefix web run build && npm --prefix api install --include=dev && cd api && npx prisma generate && npx prisma db push --accept-data-loss && npx tsc",
    "start": "npm --prefix api start"
  }
}
